---
description: Returns all values from a map expression.
---

# MAP\_VALUES

### Syntax

#### MAP\_VALUES(_input_ map) → array

* input: A map expression for which to return an array of values.

**Examples**

```sql
SELECT MAP_VALUES(properties)
-- ['Hardcover', '2002', 'Blue']
```
