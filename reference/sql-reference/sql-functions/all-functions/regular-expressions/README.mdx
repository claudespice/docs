# Regular Expressions

| Function Name                                   | Description                                                                                                              |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| [REGEXP\_EXTRACT](../string/regexp\_extract) | Extracts the first string in expression that matches the `REGEXP` expression and corresponds to the `REGEX` group index. |
| [REGEXP\_LIKE](regexp\_like)                 | Returns `true` when the specified regular expression matches values in a column. Otherwise, returns `false`.             |
| [REGEXP\_MATCHES](regexp\_matches)           | Returns `true` when the specified regular expression matches values in a column. Otherwise, returns `false`.             |
| [REGEXP\_REPLACE](../string/regexp\_replace) | Finds strings that match the given regular expression and replaces the strings with the given string.                    |
| [REGEXP\_SPLIT](../string/regexp\_split)     | Splits an input string by using a regular expression according to a keyword and an integer value.                        |
