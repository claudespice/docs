---
description: >-
  Returns the length of an input string. If the character encoding isn't
  specified, it assumes to UTF8.
---

# LENGTH

### Syntax

#### LENGTH(_expression_ varchar) → int32

* expression: String expression to determine the length of.

**Examples**

```sql
SELECT LENGTH('SPICE')
-- 5
```
